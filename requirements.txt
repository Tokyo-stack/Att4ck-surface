# ATT4ck Surface - core runtime dependencies (minimal, pinned to compatible ranges)
rich>=13.7,<15
click>=8.1,<9
pathspec>=0.12,<2

# Optional: live-target crawling / XSS probing (the `crawl` command)
#   pip install "att4ck-surface[live]"
# requests>=2.31,<3
# beautifulsoup4>=4.12,<5
